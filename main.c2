#include<stdio.h>

int Add(int x, int y);

int main() {
	int x=4, y=5;
	int sum = Add(x,y);
	printf("the sume if %i and %i is %i\n", x,y,sum);
}

int Add(int x, int y)
{
	if (y == 0)
		return x;
	else
		return Add( x ^ y, (x & y) << 1);
}

